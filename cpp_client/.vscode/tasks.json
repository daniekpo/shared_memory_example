{
    "tasks": [
        {
            "type": "shell",
            "label": "g++-7 build active file",
            "command": "make",
            "args": ["build"],
            "options": {
                "cwd": "${fileDirname}"
            }
        }
    ],
    "version": "2.0.0"
}